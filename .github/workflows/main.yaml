name: Laravel Workflow
on: push
jobs:
  build:
    name: Laravel Build
    runs-on: ubuntu-18.04    
    steps:
      - name: Action checkout git
        uses: actions/checkout@v1
      - name: Installing composer
        uses: ./.github/actions/composer
